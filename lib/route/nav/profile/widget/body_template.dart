import 'package:flutter/widgets.dart';

// TODO

class BodyTemplate extends StatelessWidget {
  const BodyTemplate({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
